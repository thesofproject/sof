		# Created with script "./bench_comp_generate.sh rtnr"
		Object.Base.route [
			{
				sink '$BENCH_PLAYBACK_DAI_COPIER'
				source 'rtnr.$BENCH_PLAYBACK_HOST_PIPELINE.1'
			}
			{
				sink 'rtnr.$BENCH_PLAYBACK_HOST_PIPELINE.1'
				source 'host-copier.0.playback'
			}
			{
				source '$BENCH_CAPTURE_DAI_COPIER'
				sink 'rtnr.$BENCH_CAPTURE_HOST_PIPELINE.2'
			}
			{
				source 'rtnr.$BENCH_CAPTURE_HOST_PIPELINE.2'
				sink 'host-copier.0.capture'
			}
		]
