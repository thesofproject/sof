		# Created with script "./bench_comp_generate.sh multiband_drc"
		Object.Base.route [
			{
				sink '$BENCH_PLAYBACK_DAI_COPIER'
				source 'multiband_drc.$BENCH_PLAYBACK_HOST_PIPELINE.1'
			}
			{
				sink 'multiband_drc.$BENCH_PLAYBACK_HOST_PIPELINE.1'
				source 'host-copier.0.playback'
			}
			{
				source '$BENCH_CAPTURE_DAI_COPIER'
				sink 'multiband_drc.$BENCH_CAPTURE_HOST_PIPELINE.2'
			}
			{
				source 'multiband_drc.$BENCH_CAPTURE_HOST_PIPELINE.2'
				sink 'host-copier.0.capture'
			}
		]
