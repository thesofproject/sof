		# Created with script "./bench_comp_generate.sh src_lite"
		Object.Base.route [
			{
				sink '$BENCH_PLAYBACK_DAI_COPIER'
				source 'src_lite.$BENCH_PLAYBACK_HOST_PIPELINE.1'
			}
			{
				sink 'src_lite.$BENCH_PLAYBACK_HOST_PIPELINE.1'
				source 'host-copier.0.playback'
			}
			{
				source '$BENCH_CAPTURE_DAI_COPIER'
				sink 'src_lite.$BENCH_CAPTURE_HOST_PIPELINE.2'
			}
			{
				source 'src_lite.$BENCH_CAPTURE_HOST_PIPELINE.2'
				sink 'host-copier.0.capture'
			}
		]
